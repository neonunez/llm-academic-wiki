---
tipo: transcripcion
fuente: "raw/parciales/1P/1.parcial_1C_2024_recuperatorio_resolucion(1).pdf"
metodo: claude_vision
parcial: 1P
cuatrimestre: 1C
año: 2024
instancia: recuperatorio
alumno: "Fialkowski, Valentín"
corrector: Perla
nota: "A"
---
# 1.parcial_1C_2024_recuperatorio_resolucion(1) - Raw Transcription

## Page 1 (Enunciado Ejercicio 1)
PLP - Recuperatorio del Primer Parcial - 1er cuatrimestre de 2024
#Orden: 81
Apellido(s): Fialkowski
Nombre(s): Valentín
Corregido por: Perla
Nota E1: B-
Nota E2: B-
Nota E3: B-
Nota Final: A

Ejercicio 1 - Programación funcional
Aclaración: en este ejercicio no está permitido utilizar recursión explícita, a menos que se indique lo contrario.
En este ejercicio vamos a modelar lógica proposicional en Haskell, de modo de poder construir fórmulas proposicionales y evaluarlas bajo distintas valuaciones.

data Prop = Var String | No Prop | Y Prop Prop | O Prop Prop | Imp Prop Prop
type Valuación = String -> Bool

Por ejemplo, la expresión: `Y (Var "P") (No (Imp (Var "Q") (Var "R")))` representa la proposición `P ^ ¬(Q => R)`.
Las valuaciones se representan como funciones que a cada variable proposicional le asignan un valor booleano. Por ejemplo la valuación `\x -> x == "P"` le asigna el valor verdadero a la variable `P` y falso a todas las otras variables proposicionales.

a) Dar el tipo y definir las funciones `foldProp` y `recProp`, que implementan respectivamente los esquemas de recursión estructural y primitiva para el tipo `Prop`. Solo en este inciso se permite usar recursión explícita.
b) Definir la función `variables :: Prop -> [String]`, que dada una fórmula devuelve la lista con todas sus variables proposicionales en algún orden, sin elementos repetidos.
Por ejemplo: `variables (O (Var "P") (No (Y (Var "Q") (Var "P"))))` debería devolver la lista `["P", "Q"]` o la lista `["Q", "P"]`.
c) Definir la función `evaluar :: Valuación -> Prop -> Bool`, que indica si una fórmula es verdadera o falsa para una valuación dada.
d) Definir la función `estáEnFNN :: Prop -> Bool`, que indica si una fórmula está en Forma Normal Negada. Es decir, si no tiene implicaciones y la negación se aplica únicamente a variables y no a proposiciones más complejas.
Por ejemplo: `Y (Var "P") (No (Imp (Var "Q") (Var "R")))` no está en FNN, y en cambio `Y (Var "P") (Y (Var "Q") (No (Var "R")))` sí lo está.

## Page 2 (Enunciado Ejercicio 2 y 3)
Ejercicio 2 - Demostración e inferencia
Considerar las siguientes definiciones sobre árboles con información en las hojas:

data AIH a = Hoja a | Bin (AIH a) (AIH a)

esHoja :: AIH a -> Bool
{E0} esHoja (Hoja x) = True
{E1} esHoja (Bin i d) = False

der :: AIH a -> AIH a
{D} der (Bin i d) = d

izq :: AIH a -> AIH a
{I} izq (Bin i d) = i

mismaEstructura :: AIH a -> AIH a -> Bool
{M0} mismaEstructura (Hoja x) = esHoja
{M1} mismaEstructura (Bin i d) = \t -> not (esHoja t) && mismaEstructura i (izq t) && mismaEstructura d (der t)

a) Demostrar la siguiente propiedad:
forall t::AIH a . forall u::AIH a . mismaEstructura t u = mismaEstructura u t
Se recomienda hacer inducción en el primer árbol, utilizando extensionalidad en el segundo. Se permite definir macros (poner nombres a expresiones largas para no tener que repetirlas).
No es obligatorio reescribir los forall correspondientes en cada paso, pero es importante recordar que están presentes y escribir los que correspondan al plantear la propiedad como predicado unario. Recordar también que los = de las definiciones pueden leerse en ambos sentidos.
Se consideran demostradas todas las propiedades conocidas sobre enteros y booleanos.

b) Usar el algoritmo W para inferir juicios de tipado válidos para las siguientes expresiones, o indicar por qué no es posible (recordar que en inferencia no está permitido renombrar variables):
i) (\x.x(\x.Succ(x)))(\x.x)
ii) \x.if isZero(x) then x else x zero

Ejercicio 3 - Cálculo Lambda Tipado
Se desea extender el cálculo lambda simplemente tipado para modelar Árboles con información en las hojas. Para eso se extienden los tipos y expresiones de la siguiente manera:
tau ::= ... | AIH(tau)
M ::= ... | Hoja(M) | Bin(M, M) | case M of Hoja x ~> M ; Bin(i, d) ~> M

- AIH(tau) es el tipo de los árboles con información en las hojas de tipo tau.
- Hoja(M) es un árbol compuesto por una única hoja con información M.
- Bin(M1, M2) es un árbol compuesto por dos subárboles M1 y M2.
- El observador case M1 of Hoja x ~> M2 ; Bin(i, d) ~> M3 permite acceder al valor de un árbol que es hoja (el cual se ligará a la variable x que puede aparecer libre en M2), y a los dos subárboles de un árbol que no es hoja (los cuales se ligarán a las variables i y d que pueden aparecer libres en M3).

a. Introducir las reglas de tipado para la extensión propuesta.
b. Definir el conjunto de valores y las nuevas reglas de semántica operacional a pequeños pasos, tanto de congruencia como de cómputo.
c. Mostrar paso por paso cómo reduce la expresión:
case (\n:Nat. Hoja(n)) Succ(zero) of Hoja x ~> Succ(Pred(x)) ; Bin(i, d) ~> zero

## Pages 3, 4, 5 (Resolución Ejercicio 1)
a)
foldProp :: (String -> a) -> (a -> a) -> (a -> a -> a) -> (a -> a -> a) -> (a -> a -> a) -> Prop -> a
foldProp fvar _ _ _ _ (Var s) = fvar s
foldProp fvar fnot fy fo fimp (Prop) = case of Prop
  (No p) = fnot (recn p)
  (Y p1 p2) = fy (recn p1) (recn p2)
  (O p1 p2) = fo (recn p1) (recn p2)
  (Imp p1 p2) = fimp (recn p1) (recn p2)
  where recn = foldProp fvar fnot fy fo fimp

recProp :: (String -> a) -> (Prop -> a -> a) -> (Prop -> Prop -> a -> a -> a) -> (Prop -> Prop -> a -> a -> a) -> (Prop -> Prop -> a -> a -> a) -> Prop -> a
recProp fvar _ _ _ _ (Var s) = fvar s
recProp fvar fnot fy fo fimp prop = case of prop:
  (No p) = fnot p (recn p)
... (similar a fold)

b)
variablesR :: Prop -> [String]
variablesR = foldProp (\s -> [s]) id (++) (++) (++)
variables :: Prop -> [String]
variables p = eliminarRepetidos (variablesR p)
eliminarRepetidos = rec id (\x xs r -> if (elem x xs) then r else [x]++r) []
(Corrector: podías usar nub, o mejor union en lugar de ++)

c)
evaluar :: Valuación -> Prop -> Bool
evaluar val = foldProp (\s -> val s) (\r -> not r) (\r1 r2 -> r1 && r2) (\r1 r2 -> r1 || r2) (\r1 r2 -> (not r1) || r2)

d)
estáEnFNN :: Prop -> Bool
estáEnFNN = recProp (const True) faux (\_ _ r1 r2 -> r1 && r2) (\_ _ r1 r2 -> r1 && r2) (\_ _ _ _ -> False)
faux :: Prop -> Bool -> Bool
faux (Var _) _ = True
faux _ _ = False

## Pages 6, 7, 8, 9, 10 (Resolución Ejercicio 2a)
Demostrar: forall t::AIH a . forall u::AIH a . mismaEstructura t u = mismaEstructura u t
Inducción sobre t.
P(t) = forall u::AIH a . mismaEstructura t u = mismaEstructura u t

Caso Base: t = Hoja n
forall u . mismaEstructura (Hoja n) u = mismaEstructura u (Hoja n)
Extensionalidad sobre u:
Si u = Hoja m:
mismaEstructura (Hoja n) (Hoja m) = esHoja (Hoja m) = True
mismaEstructura (Hoja m) (Hoja n) = esHoja (Hoja n) = True
True = True.

Si u = Bin l r:
mismaEstructura (Hoja n) (Bin l r) = esHoja (Bin l r) = False
mismaEstructura (Bin l r) (Hoja n) = not (esHoja (Hoja n)) && ... = not True && ... = False
False = False.

Paso Inductivo: t = Bin i d
HI: P(i) y P(d)
P(Bin i d): forall u . mismaEstructura (Bin i d) u = mismaEstructura u (Bin i d)
Extensionalidad sobre u.
Si u = Hoja m:
Izq: mismaEstructura (Bin i d) (Hoja m) = not (esHoja (Hoja m)) && ... = False
Der: mismaEstructura (Hoja m) (Bin i d) = esHoja (Bin i d) = False

Si u = Bin l r:
Izq: mismaEstructura (Bin i d) (Bin l r) = not (esHoja (Bin l r)) && mismaEstructura i (izq (Bin l r)) && mismaEstructura d (der (Bin l r))
= True && mismaEstructura i l && mismaEstructura d r
= mismaEstructura i l && mismaEstructura d r

Der: mismaEstructura (Bin l r) (Bin i d) = not (esHoja (Bin i d)) && mismaEstructura l (izq (Bin i d)) && mismaEstructura r (der (Bin i d))
= mismaEstructura l i && mismaEstructura r d

Por HI, mismaEstructura i l = mismaEstructura l i, y mismaEstructura d r = mismaEstructura r d.
Por lo tanto, ambas partes son iguales.

## Pages 11, 12, 13, 14, 15 (Resolución Ejercicio 2b)
Algoritmo W
i) (\x.x(\x.Succ(x)))(\x.x)
W(\x.x) -> x:X1 |- x:X1
W(\x.Succ(x)) -> x:Nat |- Succ(x):Nat  => vacio |- \x:Nat.Succ(x) : Nat -> Nat
W(x(\x.Succ(x))) -> x:(Nat->Nat)->X5 |- x(\x.Succ(x)) : X5
W(\x.x(\x.Succ(x))) -> vacio |- \x:(Nat->Nat)->X5 . x(\x:Nat.Succ(x)) : ((Nat->Nat)->X5) -> X5
W(\x.x) -> vacio |- \x:X9 . x : X9 -> X9
Aplicación: MGU entre ((Nat->Nat)->X5)->X5 y (X9->X9)->X_res
Falla? No, S = {X9 := Nat->Nat, X5 := Nat->Nat}.
(Corrector: S = {X9 = Nat->Nat, X5 = X5 -> X5 -> infinito, OJO CON LOS PARENTESIS. Falla por occur check).

ii) \x.if isZero(x) then x else x zero
W(x) -> x:X1 |- x:X1
W(isZero(x)) -> x:Nat |- isZero(x):Bool
W(x zero) -> x:Nat->X6 |- x zero : X6
Unificar if: Bool = Bool, X3 = X6, Nat = X3, Nat = Nat->X6
Falla MGU(Nat, Nat->X6) por clash/occur check.

## Pages 16, 17, 18 (Resolución Ejercicio 3)
a) Reglas de Tipado
Gamma |- M : tau
-------------------------- hojaM
Gamma |- Hoja(M) : AIH(tau)

Gamma |- M1 : AIH(tau)     Gamma |- M2 : AIH(tau)
----------------------------------------------------- Bin
Gamma |- Bin(M1, M2) : AIH(tau)

Gamma |- M : AIH(tau)   Gamma, x:tau |- M1 : sigma   Gamma, i:AIH(tau), d:AIH(tau) |- M2 : sigma
------------------------------------------------------------------------------------------------ case
Gamma |- case M of Hoja x ~> M1 ; Bin(i, d) ~> M2 : sigma

b) Semántica Operacional
Valores: V ::= ... | Hoja(V) | Bin(V, V)
Reglas:
case (Hoja V) of Hoja x ~> M1 ; Bin(i, d) ~> M2  -->  M1{x:=V}
case Bin(V1, V2) of Hoja x ~> M1 ; Bin(i, d) ~> M2  -->  M2{i:=V1, d:=V2}
Congruencia para Hoja(M), Bin(M, M2), Bin(V, M2) y case M ...

c) Reducción
case (\n:Nat. Hoja(n)) Succ(zero) of Hoja x ~> Succ(Pred(x)) ; Bin(i, d) ~> zero
-> case Hoja(n){n:=Succ(zero)} of ...  (por case3 + beta)
-> case Hoja(Succ(zero)) of Hoja x ~> Succ(Pred(x)) ; Bin(i, d) ~> zero
-> Succ(Pred(x)){x:=Succ(zero)}
-> Succ(Pred(Succ(zero)))
-> Succ(zero)
