# 1.parcial_1C_2024_resolucion(1) - Raw Transcription

## Page 1 (Enunciado Ejercicio 1 y 2 - inicio)
PLP - Primer Parcial - 1er cuatrimestre de 2024

Este examen se aprueba obteniendo al menos dos ejercicios bien menos (B-) y uno regular (R). Las notas para cada ejercicio son: -, I, R, B-, B. Entregar cada ejercicio en hojas separadas. Poner nombre, apellido y número de orden en todas las hojas, y numerarlas. Se puede utilizar todo lo definido en las prácticas y todo lo que se dio en clase, colocando referencias claras.
El orden de los ejercicios es arbitrario. Recomendamos leer el parcial completo antes de empezar a resolverlo.

Ejercicio 1 - Programación funcional
Aclaración: en este ejercicio no está permitido utilizar recursión explícita, a menos que se indique lo contrario.
El siguiente tipo de datos sirve para representar árboles ternarios:
`data AT a = NilT | Tri a (AT a) (AT a) (AT a)`
Definimos el siguiente árbol para los ejemplos:
`at1 = Tri 1 (Tri 2 NilT NilT NilT) (Tri 3 (Tri 4 NilT NilT NilT) NilT NilT) (Tri 5 NilT NilT NilT)`

a) Dar el tipo y definir la función `foldAT` que implementa el esquema de recursión estructural para el tipo `AT a`. Sólo en este inciso se permite usar recursión explícita.
b) Definir la función `preorder :: AT a -> [a]`, que lista los nodos de un árbol ternario en el orden en que aparecen: primero la raíz, después los nodos del subárbol izquierdo, luego los del medio y finalmente los del derecho.
Por ejemplo: `preorder at1 ~> [1, 2, 3, 4, 5]`.
c) Definir la función `mapAT :: (a -> b) -> AT a -> AT b`, análoga a la función `map` para listas, pero para árboles ternarios.
Por ejemplo: `mapAT (+1) at1 ~> Tri 2 (Tri 3 NilT NilT NilT) (Tri 4 (Tri 5 NilT NilT) NilT NilT) (Tri 6 NilT NilT NilT)`.
d) Definir la función `nivel :: AT a -> Int -> [a]`, que devuelve la lista de nodos del nivel correspondiente del árbol, siendo 0 el nivel de la raíz.
Por ejemplo: `nivel at1 1 ~> [2, 3, 5]`.
Pista: aprovechar la currificación y utilizar evaluación parcial.

Ejercicio 2 - Demostración de propiedades
Considerar las siguientes definiciones sobre listas y árboles estrictamente binarios:
`data AEB a = Hoja a | Bin (AEB a) a (AEB a)`

```haskell
const :: a -> b -> a
{C} const = (\x -> \y -> x)

head :: [a] -> a
{H} head (x:xs) = x

tail :: [a] -> [a]
{T} tail (x:xs) = xs

length :: [a] -> Int
{L0} length [] = 0
{L1} length (x:xs) = 1 + length xs

null :: [a] -> Bool
{N0} null [] = True
{N1} null (x:xs) = False
```

## Page 2 (Enunciado Ejercicio 2 - fin y Ejercicio 3)
```haskell
altura :: AEB a -> Int
{A0} altura (Hoja x) = 1
{A1} altura (Bin i r d) = 1 + max (altura i) (altura d)

esPreRama :: Eq a => AEB a -> [a] -> Bool
{E0} esPreRama (Hoja x) = \xs -> null xs || (xs == [x])
{E1} esPreRama (Bin i r d) = \xs -> null xs || (r == head xs && (esPreRama i (tail xs) || esPreRama d (tail xs)))
```

a) Asumiendo `Eq a`, demostrar la siguiente propiedad:
`forall t::AEB a . forall xs::[a] . esPreRama t xs => length xs <= altura t`

Se recomienda hacer inducción en el árbol, utilizando extensionalidad de booleanos y listas cuando sea necesario. Se permite definir macros (i.e., poner nombres a expresiones largas para no tener que repetirlas).
No es obligatorio escribir los forall correspondientes en cada paso, pero es importante recordar que están presentes. Recordar también que los = de las definiciones pueden leerse en ambos sentidos.
Se consideran demostradas todas las propiedades conocidas sobre enteros y booleanos, así como también que `forall t::AEB a . altura t >= 0`.

b) Demostrar el siguiente teorema usando deducción natural, sin utilizar principios clásicos:
`((P => Q) ^ (Q => R)) => ¬R => ¬P`

Ejercicio 3 - Cálculo Lambda Tipado
Se desea extender el cálculo lambda simplemente tipado para modelar Diccionarios. Para eso se extienden los tipos y expresiones de la siguiente manera:
`tau ::= ... | Dicc(sigma, tau)`
`M ::= ... | Vacio_{sigma,tau} | definir(M, M, M) | def?(M, M) | obtener(M, M)`

- `Dicc(sigma, tau)` es el tipo de los diccionarios con claves de tipo `sigma` y valores de tipo `tau`.
- `Vacio_{sigma,tau}` es un diccionario vacío con claves de tipo `sigma` y valores de tipo `tau`.
- `definir(M, N, O)` define el valor `O` en el diccionario `M` para la clave `N`.
- `def?(M, N)` indica si la clave `N` fue definida en el diccionario `M`.
- `obtener(M, N)` da el valor asociado a la clave `N` en el diccionario `M` (se espera que el diccionario tenga definida la clave, en caso contrario, la expresión puede tipar, pero no se obtendrá un valor).

a. Introducir las reglas de tipado para la extensión propuesta.

b. Definir el conjunto de valores y las nuevas reglas de semántica operacional a pequeños pasos. Suponer que el tipo de las claves cuenta con el operador `==` (es decir, el cálculo está extendido con un operador de comparación para los tipos que se usen como claves). Para el caso de `obtener(M, N)`, se espera que la clave `N` esté definida en el diccionario `M`. Si no lo está, puede colgarse o terminar en una expresión de error (una forma normal que no es un valor). No es necesario escribir las reglas de congruencia, sino que basta con indicar cuántas son.

c. Mostrar paso por paso cómo reduce la expresión:
`(lambda d: Dicc(Nat, Bool). if def?(d, 0) then obtener(d, 0) else False) definir(Vacio_{Nat,Bool}, 0, True)`
Suponer que `zero == zero -> True`.

## Page 3 (Resolución Ejercicio 1)
```
OTAZUA ARCE, MATEO (88/23) (0:185)
HOJA 1/4
Ej | 1 | 2 | 3
   | B | R | B-

Aprobado

Corrigió Daniela

Ejercicio 1

a) foldAT :: b -> (a -> b -> b -> b -> b) -> AT a -> b
foldAT cNil cTri t = case t of
    NilT -> cNil
    Tri x i m d -> cTri x (rec i) (rec m) (rec d)
        where rec = foldAT cNil cTri

b) preorder :: AT a -> [a]
preorder = foldAT [] (\x ri rm rd -> x : (ri ++ rm ++ rd))

c) mapAT :: (a -> b) -> AT a -> AT b
mapAT f = foldAT (NilT) (\x ri rm rd -> Tri (f x) ri rm rd)

d) nivel :: AT a -> Int -> [a]
nivel = foldAT (\i -> []) (\x ri rm rd -> \i ->
    if i == 0
    then [x]
    else ((ri (i-1)) ++ (rm (i-1)) ++ (rd (i-1))))
```

## Page 4 (Resolución Ejercicio 2.a parte 1)
```
OTAZUA ARCE, MATEO (88/23) (0:185)
HOJA 2/4

Ejercicio 2

a) Se nos pide demostrar la siguiente propiedad:
forall t: AEB a ::
  forall xs: [a] ::
    esPreRama t xs -> length xs <= altura t

Para demostrarla, hacemos inducción estructural sobre el árbol (AEB). Tiene dos constructores: Hoja y Bin.
Empezamos por el CASO BASE (Hoja h). forall h: a :: P(Hoja h).

Defino P(x) = forall xs: [a] :: esPreRama x xs -> length xs <= altura x.

P(Hoja h) = forall xs: [a] :: esPreRama (Hoja h) xs -> length xs <= altura (Hoja h)
Reemplazo por {A0} y {E0}.
forall xs: [a] :: (\xs -> null xs || (xs == [h])) xs -> length xs <= 1
Aplico beta
forall xs: [a] :: null xs || (xs == [h]) -> length xs <= 1
Nota corrector: "No hacía falta, se podía usar extensionalidad (lo aclara la consigna)"
Para demostrar esto hacemos inducción sobre listas. Definimos la anterior expresión sin el forall como Q(xs).

CASO BASE Q([])
null [] || ([] == [h]) -> length [] <= 1
Reemplazamos por {N0} y {L0}
True || ([] == [h]) -> 0 <= 1
Simplificamos || y <=
True -> True
Queda demostrado Q([]).

PASO INDUCTIVO Q(ys) -> Q(y:ys)
Asumimos Q(ys). Demostramos Q(y:ys).
null (y:ys) || ((y:ys) == [h]) -> length (y:ys) <= 1
Reemplazamos por {N1} y {L1}.
False || ((y:ys) == [h]) -> 1 + length (ys) <= 1
Simplificamos ||
(y:ys) == [h] -> 1 + length (ys) <= 1
Ahora podemos separar en dos casos: ys es lista vacía o ys tiene al menos un elemento.
Si ys es vacía, reemplazando por {L0} en la derecha, nos queda la tautología 1 <= 1, así que no importaría lo que quede a la izquierda de la implicación.
Si ys tiene al menos un elemento, entonces (y:ys) == [h] nunca podría ser verdadero, así que no importaría lo que quede del lado derecho.
```

## Page 5 (Resolución Ejercicio 2.a parte 2)
```
Habiendo demostrado Q([]) y Q(y:ys), queda demostrado forall h:a :: P(Hoja h).

Ahora avanzamos con el PASO INDUCTIVO (Bin i r d).
(forall i, d: AEB a :: P(i) ^ P(d)) -> (forall r:a :: P(Bin i r d))
Asumimos verdadero la izquierda de la implicación.
P(Bin i r d) = forall xs: [a] ::
  esPreRama (Bin i r d) xs -> length xs <= altura (Bin i r d)
Veamos por separado los lados de la implicación. Primero la izquierda.
esPreRama (Bin i r d) xs
{E1} + beta
null xs || (r == head xs && (esPreRama i (tail xs) || esPreRama d (tail xs)))

Ahora la derecha.
length xs <= altura (Bin i r d)
{A1}
length xs <= 1 + max (altura i) (altura d)

Juntando ambas partes queda
forall xs: [a] ::
null xs || (r == head xs && (esPreRama i (tail xs) || esPreRama d (tail xs))) -> length xs <= 1 + max (altura i) (altura d)

Para demostrar esto aplicamos extensionalidad sobre listas:

CASO BASE xs = []
null [] || (r == head [] && (esPreRama i (tail []) || esPreRama d (tail []))) -> length [] <= 1 + max (altura i) (altura d)
{N0}, {L0} y ||
True -> 0 <= 1 + max (altura i) (altura d)
Como forall t: AEB :: altura t >= 0, lo de la derecha es una tautología y vale entonces la implicación.

CASO xs = y:ys
null (y:ys) || (r == head (y:ys) && (esPreRama i (tail (y:ys)) || esPreRama d (tail (y:ys)))) -> length (y:ys) <= 1 + max (altura i) (altura d)
{N1}, {H}, {T}, {L1}
(r == y && (esPreRama i ys || esPreRama d ys)) -> 1 + length ys <= 1 + max (altura i) (altura d)
Nota corrector: "¿Extensionalidad de booleanos?"
Ahora tenemos dos casos: r = y, r != y
Si r != y, lo de la izquierda es siempre falso, y vale la implicación.
Si r = y
etc ??

Incompleto.
```

## Page 6 (Resolución Ejercicio 2.b)
```
OTAZUA ARCE, MATEO (88/23) (0:185)
HOJA 3/4

Ejercicio 2
b)

Incompleto.

  Gamma               Gamma
-----------         ------------
Gamma |- P => Q     Gamma |- Q => R
----------------------------------- ^e2
Gamma |- P => R                              ------- ax
------------------ ->e                       Gamma |- ¬R
Gamma |- P => R                              ------------- MT
-------------------------------------------------------
Gamma = (P => Q) ^ (Q => R), ¬R |- ¬P
------------------------------------------------------- ->i
(P => Q) ^ (Q => R) |- ¬R => ¬P
------------------------------------------------------- ->i
|- ((P => Q) ^ (Q => R)) => ¬R => ¬P
```

## Page 7 (Resolución Ejercicio 3.a y 3.b)
```
OTAZUA ARCE, MATEO (88/23) (0:185)
HOJA 4/4

Ejercicio 3

tau ::= ... | Dicc(sigma, tau)
M ::= ... | Vacio_{sigma,tau} | definir(M, M, M) | def?(M, M) | obtener(M, M)

a) REGLAS DE TIPADO

--------------------------------- ax_vacio
Gamma |- Vacio_{sigma,tau} : Dicc(sigma, tau)

Gamma |- M : Dicc(sigma, tau)    Gamma |- N : sigma    Gamma |- O : tau
------------------------------------------------------------------ definir
Gamma |- definir(M, N, O) : Dicc(sigma, tau)

Gamma |- M : Dicc(sigma, tau)    Gamma |- N : sigma
--------------------------------------------------- def?
Gamma |- def?(M, N) : Bool

Gamma |- M : Dicc(sigma, tau)    Gamma |- N : sigma
--------------------------------------------------- obtener
Gamma |- obtener(M, N) : tau


b)
V ::= ... | Vacio_{sigma,tau} | definir(V, V, V)

SEMÁNTICA OPERACIONAL

(def?V)   def?(Vacio_{sigma,tau}, W) -> False
(def?X)   def?(definir(V, W, U), W') -> if W == W' then True else def?(V, W')
(obtX)    obtener(definir(V, W, U), W') -> if W == W' then U else obtener(V, W')
(obtV)    obtener(Vacio_{sigma,tau}, W) -> obtener(Vacio_{sigma,tau}, W)  // (se cuelga)

y hay 7 reglas de congruencia: 3 para definir(), 2 para def? y 2 para obtener()
```

## Page 8 (Resolución Ejercicio 3.c)
```
C) Reducimos la siguiente expresión:
(lambda d: Dicc(Nat, Bool). if def?(d, 0) then obtener(d, 0) else False) definir(Vacio_{Nat,Bool}, 0, True)

(beta)
-> (if def?(d, 0) then obtener(d, 0) else False) {d := definir(Vacio_{Nat,Bool}, 0, True)}

-> if def?(definir(Vacio_{Nat,Bool}, 0, True), 0) then obtener(definir(Vacio_{Nat,Bool}, 0, True), 0) else False

(if_c + def?X)
-> if (if 0 == 0 then True else def?(Vacio_{Nat,Bool}, 0)) then obtener(definir(Vacio_{Nat,Bool}, 0, True), 0) else False

(if_c + if_t + 0==0 + if_t)
Nota corrector: SALTEA PASOS (* debería ser -> if (if true then ...) then ...)
-> if True then obtener(definir(Vacio_{Nat,Bool}, 0, True), 0) else False

(if_t)
-> obtener(definir(Vacio_{Nat,Bool}, 0, True), 0)

(obtX)
-> if 0 == 0 then True else obtener(Vacio_{Nat,Bool}, 0)

(0==0 + if_t)
-> True
```
